---
- name: Setup showroom content symlinks
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Create antora symlink for nookbag SPA content discovery
      ansible.builtin.file:
        src: /showroom/www
        dest: /showroom/www/antora
        state: link
